#include<stdio.h>
int main(){
    int i = 10;
    int j = 20;
    // %d id format specifier
    // %d is for int, %f is for float and %c is for character
    printf("The value of i is %d and the value of j is: %d \n",i,j);
    printf("Thanks!!");
    return 0;
}