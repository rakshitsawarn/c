#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n;
    int *ptr;
    scanf("%d",&n);
    int arr[n];
    scanf("%d",&n);
    ptr[0] = 3;
    ptr = (int *)malloc(n*sizeof(int));
    //int ar[n]; Not avaible in c
    return 0;
}